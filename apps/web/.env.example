# Public URL of the marketing site, used for canonical URLs and metadata.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Infrastructure values (submissions API URL, table name) are NOT environment
# variables — they arrive at runtime through SST `Resource` bindings (Phase 3).
