# @repo/auth

Shared authentication logic for the agency platform.

## Status

🚧 **Placeholder** — this package is scaffolded but not yet implemented.

## Planned Features

- Session management with secure HTTP-only cookies
- OAuth providers (Google, GitHub) via Auth.js or similar
- Role-based access control (RBAC)
- Server-side session validation middleware
- Zod schemas for type-safe auth data validation

## Usage (future)

```ts
import { createAuthClient, validateSession } from '@repo/auth';
```
